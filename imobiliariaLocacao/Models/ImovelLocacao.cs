//namespace imobiliariaLocacao.Models
//{
  //  public class ImovelLocacao
    //{
      //  private string connectionString = "string de conexão com o banco de dados";

        //public List<Imovel> Listar()
       // {
          //  List<Imovel> imoveis = new List<Imovel>();

            //using (SqlConnection connection = new SqlConnection(connectionString))
            //{
              //  connection.Open();

                //SqlCommand command = new SqlCommand("SELECT * FROM Imoveis", connection);

                //SqlDataReader reader = command.ExecuteReader();

                //while (reader.Read())
             //   {
                  //  Imovel imovel = new Imovel();

                    //imovel.Id = (int)reader["Id"];
                    //imovel.Endereco = (string)reader["Endereco"];
                   // imovel.Bairro = (string)reader["Bairro"];
                    //imovel.Cidade = (string)reader["Cidade"];
                   // imovel.Estado = (string)reader["Estado"];
                  //  imovel.Cep = (string)reader["Cep"];
                  //  imovel.ValorAluguel = (decimal)reader["ValorAluguel"];

                   // imoveis.Add(imovel);
         //       }
       //     }

      //      return imoveis;
      //  }

      //  public void Inserir(Imovel imovel)
      //  {
      //      using (SqlConnection connection = new SqlConnection(connectionString))
      //      {
       //         connection.Open();

      //          SqlCommand
      //  }
//}
